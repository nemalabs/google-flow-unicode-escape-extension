{
  "name": "unicode-escape-for-google-labs-fx",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "bun build src/content.ts --outdir dist --target=browser --format=iife",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "jsdom": "^25.0.0"
  }
}
